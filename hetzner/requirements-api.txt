# Search-service deps (the box that runs search_api.py). Install the CPU torch
# build -- text/image query encoding is a single forward pass, no GPU needed:
#   pip install -r requirements-api.txt --extra-index-url https://download.pytorch.org/whl/cpu
torch
open_clip_torch
pillow
numpy
fastapi
python-multipart        # required by FastAPI for the POST /search image upload
uvicorn[standard]
psycopg[binary]
psycopg_pool
